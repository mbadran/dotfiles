# user-wide claude code instructions

See **AGENTS.md** for general agent instructions that apply across every
project. Project-specific overlays (`<repo>/AGENTS.md`, `<repo>/CLAUDE.md`)
take precedence where they conflict.
